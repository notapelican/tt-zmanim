# TTCC sheet service dependencies.
# python-docx is required even for the HTML-only path: engine/render_html.py
# imports shared constants from engine/render_docx.py at module load, which
# imports python-docx. It is also used by the optional .docx export.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2
playwright>=1.40
python-docx>=1.2.0
# test-only:
httpx>=0.27
